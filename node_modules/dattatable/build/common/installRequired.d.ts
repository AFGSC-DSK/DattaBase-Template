import { Components, Helper } from "gd-sprest-bs";
export interface IShowDialogProps {
    errors?: Components.IListGroupItem[];
    onHeaderRendered?(el: HTMLElement): any;
    onBodyRendered?(el: HTMLElement): any;
    onFooterRendered?(el: HTMLElement): any;
}
/**
 * Installation Required
 * Checks the SharePoint configuration file to see if an install is required.
 */
export declare class InstallationRequired {
    private static _cfg;
    private static _report;
    private static _listsExist;
    static get ListsExist(): boolean;
    private static checkLists;
    static requiresInstall(cfg: Helper.ISPConfig): PromiseLike<boolean>;
    static showDialog(props?: IShowDialogProps): void;
}
